// React deps
import React from 'react';

const TabsContent = (props) => {
    return (

        <section className="tabs">

            {props.children}

        </section>

    );
}

export default TabsContent;